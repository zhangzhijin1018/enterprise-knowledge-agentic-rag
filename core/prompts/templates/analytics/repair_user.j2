## 原始用户意图

```json
{{ original_intent | tojson }}
```

## SQL 执行错误

**错误类型**: {{ error_type }}

**错误信息**: {{ error_message }}

## 修复指南

请根据错误类型选择合适的修复策略：

### 1. timeout（超时）
- 如果时间范围太精确（如指定了具体日期），放宽为月份或季度
- 如果分组维度太细（如电站级别），放宽为区域级别
- 如果包含了同比/环比分析，移除这些复杂计算

### 2. no_data（无数据）
- 检查时间范围是否正确
- 检查指标名称是否正确
- 放宽时间范围或简化分组

### 3. too_many_rows（返回行数过多）
- 减少 top_n 值
- 简化分组维度

### 4. syntax_error（语法错误）
- 这种错误通常表示意图本身有问题，需要用户澄清

## 输出要求

请分析错误原因，选择最合适的修复策略，并输出 JSON 格式的修复结果。
