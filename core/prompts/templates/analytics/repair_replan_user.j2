## 原始用户意图

```json
{{ original_intent | tojson }}
```

## 错误信息

{{ error_message }}

## 会话历史

{% if conversation_memory %}
```json
{{ conversation_memory | tojson }}
```
{% else %}
无会话历史
{% endif %}

## 重新规划指南

请分析用户的原始意图和错误原因，重新规划一个可行的分析方案：

1. 如果原始时间范围太精确，改为更宽泛的时间范围
2. 如果原始分组维度太细，改为更粗粒度的维度
3. 如果包含复杂的同比/环比分析，暂时移除这些分析
4. 如果问题仍然无法解决，生成澄清请求

## 输出要求

请输出 JSON 格式的重新规划结果。
